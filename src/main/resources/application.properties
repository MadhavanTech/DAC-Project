spring.application.name=My_DAC_Project

# Change server.port if default 8080 is already in use.
server.port=8081

# Optional useful dev properties
spring.main.banner-mode=console
logging.level.org.springframework=INFO
logging.level.My_DAC_Project=DEBUG